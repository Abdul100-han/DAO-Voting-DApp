[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.28"
ignored_warnings_from = ["lib"]

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"

[etherscan]
sepolia = { key = "${ETHERSCAN_API_KEY}" }
